spring:
  datasource:
    url: jdbc:tc:postgresql:17.9-alpine:///

  jpa:
    hibernate:
      ddl-auto: validate

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.xml

  test:
    database.replace: none
